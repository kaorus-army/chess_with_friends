<h1>Game Index View</h1>

<ul>
  <% @games.each do |game| %>
    <li><%= link_to game.nickname, game_url(game) %></li>
  <% end %>
</li>

<p><%= link_to "Start A New Game", new_game_url, class: 'btn btn-primary' %></p>
